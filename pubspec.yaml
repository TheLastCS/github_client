name: github_client
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.0-444.0.dev <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  github: ^9.7.0
  http: ^0.13.5
  oauth2: ^2.0.1
  url_launcher: ^6.1.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
